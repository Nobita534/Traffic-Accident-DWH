name: 'traffic_accident_dwh'
version: '1.0.0'
config-version: 2


profile: 'traffic_project_profile'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"


models:
  traffic_accident_dwh:
    staging:
      materialized: view
    intermediate:
      materialized: view
    marts:
      materialized: table